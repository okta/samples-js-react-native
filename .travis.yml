os: linux
cache: false

install:
- yarn install --frozen-lockfile

jobs:
  include:
    - language: node_js
      node_js: 14.15
      stage: Unit tests
      name: React Native
      script:
      - yarn test

